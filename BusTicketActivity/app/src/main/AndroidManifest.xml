<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.busticketactivity">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_round"
        android:supportsRtl="true"
        tools:replace="android:allowBackup"
        android:theme="@style/AppTheme">
        <activity android:name=".payment.PaymentActivity">

        </activity>
        <activity android:name=".pickticket.PickTicketActivity">

        </activity>
        <activity android:name=".tiketmenu.TiketActivty">

        </activity>
        <activity android:name=".home.HomeActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".regist.RegistAddImageActivity" />
        <activity android:name=".regist.RegisterActivity" />
        <activity android:name=".signin.SignInActivity" />
        <activity android:name=".signin.SignInAndRegistActivity" />
        <activity android:name=".ui.MainActivity">

        </activity>
    </application>

</manifest>